<!-- views/partials/navbar.ejs -->
<nav class="bg-white/70 backdrop-blur-md shadow-md sticky top-0 z-50">
  <div class="max-w-6xl mx-auto px-6 py-4 flex justify-between items-center">
    <!-- Logo -->
    <a href="/" class="flex items-center space-x-2 group">
      <h1
        class="text-3xl font-extrabold text-indigo-600 tracking-tight group-hover:text-indigo-700 transition"
      >
        My Demo App
      </h1>
    </a>

    <!-- Navigation Links -->
    <ul class="flex space-x-6 text-lg">
      <li>
        <a
          href="/dashboard"
          class="<%= currentPath.startsWith('/dashboard') ? 'text-indigo-600 font-semibold border-b-2 border-indigo-600 pb-1' : 'text-gray-700 hover:text-indigo-600 transition' %>"
        >
          Dashboard
        </a>
      </li>
      <li>
        <a
          href="/auth/login"
          class="<%= currentPath === '/auth/login' || currentPath === '/auth/register' ? 'text-indigo-600 font-semibold border-b-2 border-indigo-600 pb-1' : 'text-gray-700 hover:text-indigo-600 transition' %>"
        >
          Login / Register
        </a>
      </li>
      <li>
        <a
          href="/profile"
          class="<%= currentPath.startsWith('/profile') ? 'text-indigo-600 font-semibold border-b-2 border-indigo-600 pb-1' : 'text-gray-700 hover:text-indigo-600 transition' %>"
        >
          Profile
        </a>
      </li>
    </ul>
  </div>
</nav>
